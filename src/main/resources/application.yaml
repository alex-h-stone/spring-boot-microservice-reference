server:
  port: 8080
  shutdown: graceful

management:
  server:
    port: 8099
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,mappings
  endpoint:
    health:
      show-details: always

spring:
  data:
    mongodb:
      host: alex-stone-cosmos-for-mongo-db.mongo.cosmos.azure.com
      port: 10255
      database: database1_as
      username: alex-stone-cosmos-for-mongo-db
      password: e7dxmlYipYh4UQopl1CY6fCWEFesIJkTAWicDtjDQQ1xu548TEheQ08apuQ1WxQuufKAYX9PlUPBACDbtFokyQ==
      maxIdleTimeMS: 30000
      ssl:
        enabled: true

logging:
  level:
    org:
      springframework:
        security: DEBUG
        web: DEBUG

thirdparty:
  apis:
    vaccinations:
      path: "/vaccinations/{vaccinationId}"
      scheme: "http"
      host: "www.vaccinations.thirdparty.com"
      port: 9999
